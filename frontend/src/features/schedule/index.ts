/**
 * Schedule Feature Exports
 *
 * Components specific to the /schedule (booking) page.
 *   - ScheduleHeroSection: the hero area with a "Book a session" call to action
 *   - LearnMoreModal: the popup modal triggered from the hero section
 */
export { ScheduleHeroSection } from "./ScheduleHeroSection";
export { LearnMoreModal } from "./LearnMoreModal";
